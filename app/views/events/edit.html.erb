<%= render "header" %>

<div class="container">
  <div class="column-container flex justify-between">
    <div class="column column--small mb-50">
      <h2 class="heading-xl sm:heading-xl mb-20">Edit Your Table</h2>
    </div>
    <div class="column column--big flex-col">
      <%= form_for(@event) do |f| %>
        <% if @event.errors.any? %>
          <% @event.errors.full_messages.each do |msg| %>
            <p class="alert">Error: <%= msg %></p>
          <% end %>
        <% end %>
        <%= render 'event_fields',
            f: f %>
        <p>
          <%= f.submit "Save Changes",
              class: "btn btn--primary" %>
        </p>
      <% end %>
    </div>
  </div>
</div>
