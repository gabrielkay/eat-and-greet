<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <% @event.errors.full_messages.each do |msg| %>
      <div class="alert"><p>Error: <%= msg %></p></div>
    <% end %>
  <% end %>
  <%= render 'event_fields', f: f %>
  <p>
    <%= f.submit "Create Event" %>
  </p>
<% end %>
